# Configurações do Sistema de Comunicados
# Copie este arquivo para .env e ajuste os valores conforme necessário

# Chave secreta do Flask (gerada automaticamente se não fornecida)
# Gere uma nova chave com: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=

# URI do banco de dados
# SQLite (padrão): sqlite:///comunicados.db
# PostgreSQL: postgresql://user:password@localhost/dbname
# MySQL: mysql://user:password@localhost/dbname
DATABASE_URI=sqlite:///comunicados.db

# Pasta de uploads
UPLOAD_FOLDER=static/uploads

# Configurações do servidor Flask
# Para produção, defina FLASK_DEBUG=false
FLASK_DEBUG=false
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
